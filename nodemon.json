{
  "ignore": ["**/*.test.ts", "**/*.spec.ts", ".git", "node_modules"],
  "watch": ["src"],
  "exec": "npm run build && node --inspect -r ts-node/register ./src/index.ts",
  "ext": "ts",
  "verbose": true,
  "execMap": {
    "ts": "node --require ts-node/register"
  }
}
